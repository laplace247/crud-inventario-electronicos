<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Detalles del Producto</title>
    <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
    <%- include('../partials/header.ejs') %>

    <div class="container">
        <h1>Detalles del Producto</h1>
        <div class="card">
            <div class="card-body">
                <p class="card-text"><strong>Nombre: </strong><%= producto.nombre %></p>
                <p class="card-text"><strong>Precio: </strong> $<%= producto.precio %></p>
                <p class="card-text"><strong>Stock: </strong> <%= producto.stock %></p>
                <% if (producto.imagen) { %>
                    <strong>Imagen: </strong>
                    <br>
                    <img src="/uploads/<%= producto.imagen %>" alt="<%= producto.nombre %>" class="img-fluid"
                        style="width: 150px; height: 150px; object-fit: cover; border-radius: 8px; border: 1px solid #ccc;" />
                <% } %>
                <p>
                    <a href="/productos/listar" class="btn btn-secondary mt-3">Volver</a>
                </p>    
            </div>
        </div>
    </div>

    <%- include('../partials/footer.ejs') %>
</body>
</html>