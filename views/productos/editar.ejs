<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Editar Producto</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partials/header') %>

    <div class="container mt-5">
        <h1>Editar Producto</h1>
        <form action="/productos/actualizar/<%= producto.id %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="nombre" class="form-label">Nombre:</label>
                <input type="text" class="form-control" id="nombre" name="nombre" value="<%= producto.nombre %>" required>
            </div>
            <div class="mb-3">
                <label for="precio" class="form-label">Precio:</label>
                <input type="number" step="0.01" class="form-control" id="precio" name="precio" value="<%= producto.precio %>" required>
            </div>
            <div class="mb-3">
                <label for="stock" class="form-label">Stock:</label>
                <input type="number" class="form-control" id="stock" name="stock" value="<%= producto.stock %>" required>
            </div>

            <div class="mb-3">
                <label class="form-label">Imagen:</label><br>
                <% if (producto.imagen) { %>
                    <img src="/uploads/<%= producto.imagen %>" alt="" style="width: 150px; height: 150px; object-fit: cover; border-radius: 8px; border: 1px solid #ccc;"" width="150"><br>
                    <!-- Campo oculto para mantener la imagen si no se sube una nueva -->
                    <input type="hidden" name="imagenActual" value="<%= producto.imagen %>">
                <% } else { %>
                    <p>No hay imagen disponible.</p>
                <% } %>
            </div>

            <div class="mb-3">
                <label for="imagen" class="form-label">Selecciona una imagen:</label>
                <input type="file" class="form-control" id="imagen" name="imagen" accept="image/*">
            </div>

            <button type="submit" class="btn btn-success">Actualizar Producto</button>
            <a href="/productos/listar" class="btn btn-secondary">Volver</a>
        </form>
    </div>

    <%- include('../partials/footer') %>
</body>
</html>
